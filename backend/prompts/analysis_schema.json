{
  "type": "object",
  "properties": {
    "summary": {
      "type": "string",
      "description": "A long-form, detailed narrative summary (2-4 paragraphs). Describe the context, the applications and tools being used, the state of their workspace, their apparent focus and intent. Mention visible file names, project directories, code snippets, UI states, terminal output, and any other relevant details. Be specific and concrete."
    },
    "events": {
      "type": "array",
      "description": "Specific actions or events inferred from the screenshots.",
      "items": {
        "type": "object",
        "properties": {
          "event": {
            "type": "string",
            "description": "A specific action or event inferred. 1-2 descriptive sentences."
          },
          "confidence": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Confidence score 1-10. 10 = absolutely certain from visual evidence."
          },
          "evidence": {
            "type": "string",
            "description": "1-2 sentences citing specific visual details from the screenshots that support this."
          }
        },
        "required": ["event", "confidence", "evidence"],
        "additionalProperties": false
      },
      "minItems": 2,
      "maxItems": 4
    },
    "personalities": {
      "type": "array",
      "description": "Personality traits observed in the user.",
      "items": {
        "type": "object",
        "properties": {
          "personality": {
            "type": "string",
            "description": "A personality trait observed. 1-2 descriptive sentences."
          },
          "confidence": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Confidence score 1-10."
          },
          "evidence": {
            "type": "string",
            "description": "1-2 sentences citing visual details that support this observation."
          }
        },
        "required": ["personality", "confidence", "evidence"],
        "additionalProperties": false
      },
      "minItems": 2,
      "maxItems": 5
    },
    "skills": {
      "type": "array",
      "description": "Technical skills or abilities demonstrated.",
      "items": {
        "type": "object",
        "properties": {
          "skill": {
            "type": "string",
            "description": "A technical skill demonstrated. 1-2 descriptive sentences."
          },
          "confidence": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Confidence score 1-10."
          },
          "evidence": {
            "type": "string",
            "description": "1-2 sentences citing visual details that support this."
          }
        },
        "required": ["skill", "confidence", "evidence"],
        "additionalProperties": false
      },
      "minItems": 1,
      "maxItems": 3
    },
    "interests": {
      "type": "array",
      "description": "Topics or domains the user is actively engaged with.",
      "items": {
        "type": "object",
        "properties": {
          "interest": {
            "type": "string",
            "description": "A topic or domain of active engagement. 1-2 descriptive sentences."
          },
          "confidence": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Confidence score 1-10."
          },
          "evidence": {
            "type": "string",
            "description": "1-2 sentences citing visual details that support this."
          }
        },
        "required": ["interest", "confidence", "evidence"],
        "additionalProperties": false
      },
      "minItems": 1,
      "maxItems": 3
    },
    "preferences": {
      "type": "array",
      "description": "Tool, workflow, or UI choices the user appears to prefer.",
      "items": {
        "type": "object",
        "properties": {
          "preference": {
            "type": "string",
            "description": "A tool, workflow, or UI preference observed. 1-2 descriptive sentences."
          },
          "confidence": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Confidence score 1-10."
          },
          "evidence": {
            "type": "string",
            "description": "1-2 sentences citing visual details that support this."
          }
        },
        "required": ["preference", "confidence", "evidence"],
        "additionalProperties": false
      },
      "minItems": 1,
      "maxItems": 3
    },
    "ownerships": {
      "type": "array",
      "description": "Projects, tools, or assets the user appears to own or be responsible for. Empty array if unclear.",
      "items": {
        "type": "object",
        "properties": {
          "ownership": {
            "type": "string",
            "description": "A project, tool, or asset owned. 1-2 descriptive sentences."
          },
          "confidence": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Confidence score 1-10."
          },
          "evidence": {
            "type": "string",
            "description": "1-2 sentences citing visual details that support this."
          }
        },
        "required": ["ownership", "confidence", "evidence"],
        "additionalProperties": false
      },
      "minItems": 0,
      "maxItems": 3
    },
    "relationships": {
      "type": "array",
      "description": "People, teams, services, or organizations the user relates to. Empty array if unclear.",
      "items": {
        "type": "object",
        "properties": {
          "relationship": {
            "type": "string",
            "description": "A relationship the user has. 1-2 descriptive sentences."
          },
          "confidence": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Confidence score 1-10."
          },
          "evidence": {
            "type": "string",
            "description": "1-2 sentences citing visual details that support this."
          }
        },
        "required": ["relationship", "confidence", "evidence"],
        "additionalProperties": false
      },
      "minItems": 0,
      "maxItems": 3
    },
    "weaknesses": {
      "type": "array",
      "description": "Potential areas of inefficiency, risk, or struggle. Be constructive.",
      "items": {
        "type": "object",
        "properties": {
          "weakness": {
            "type": "string",
            "description": "A potential inefficiency or risk observed. Be constructive. 1-2 descriptive sentences."
          },
          "confidence": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Confidence score 1-10."
          },
          "evidence": {
            "type": "string",
            "description": "1-2 sentences citing visual details that support this."
          }
        },
        "required": ["weakness", "confidence", "evidence"],
        "additionalProperties": false
      },
      "minItems": 1,
      "maxItems": 3
    },
    "goals": {
      "type": "array",
      "description": "Abstract, higher-level objectives the user appears to be working toward.",
      "items": {
        "type": "object",
        "properties": {
          "goal": {
            "type": "string",
            "description": "An abstract objective inferred. 1-2 descriptive sentences."
          },
          "confidence": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "description": "Confidence score 1-10."
          },
          "evidence": {
            "type": "string",
            "description": "1-2 sentences citing visual details that support this."
          }
        },
        "required": ["goal", "confidence", "evidence"],
        "additionalProperties": false
      },
      "minItems": 1,
      "maxItems": 3
    }
  },
  "required": [
    "summary",
    "events",
    "personalities",
    "skills",
    "interests",
    "preferences",
    "ownerships",
    "relationships",
    "weaknesses",
    "goals"
  ],
  "additionalProperties": false
}
